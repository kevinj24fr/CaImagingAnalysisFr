% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ca_experiment.R
\name{CaExperiment}
\alias{CaExperiment}
\alias{CaExperiment-class}
\title{CaExperiment: Unified Calcium Imaging Analysis Object}
\usage{
CaExperiment(
  traces,
  movie = NULL,
  rois = NULL,
  meta.data = NULL,
  frame_rate = 30,
  experiment_id = NULL,
  ...
)
}
\arguments{
\item{traces}{A numeric matrix of calcium traces (cells x timepoints) or CalciumTraces object}

\item{movie}{Optional CalciumMovie object}

\item{rois}{Optional ROISet object with cell spatial information}

\item{meta.data}{Optional data frame with cell-level metadata}

\item{frame_rate}{Imaging frame rate in Hz}

\item{experiment_id}{Unique identifier for the experiment}

\item{...}{Additional metadata to store}
}
\value{
A CaExperiment object
}
\description{
A Seurat-style unified object for calcium imaging analysis workflows.
Stores all data, analysis results, and maintains full provenance tracking.

A unified container for calcium imaging analysis that stores raw data,
processed traces, spike inference results, dimensionality reductions,
network graphs, and metadata in a single object with full provenance tracking.
}
\examples{
\dontrun{
# Create from trace matrix
traces <- matrix(rnorm(100 * 1000), nrow = 100)
ca <- CaExperiment(traces, frame_rate = 30)

# Analysis pipeline with method chaining
ca <- ca |>
  RunCorrection(method = "neuropil") |>
  RunDFF() |>
  RunSpikes(method = "oasis") |>
  RunPCA(n_components = 20) |>
  RunConnectivity() |>
  RunAssemblies()

# Access results
GetTraces(ca, assay = "dff")
GetSpikes(ca)
GetReduction(ca, "pca")
}
}
