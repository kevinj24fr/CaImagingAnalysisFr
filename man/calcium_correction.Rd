% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcium_correction.R
\name{calcium_correction}
\alias{calcium_correction}
\title{Calcium Trace Correction}
\usage{
calcium_correction(
  raw_df,
  method = c("modern", "legacy"),
  span = NULL,
  normalize = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{raw_df}{A data frame containing calcium traces with columns named
\verb{Cell_*} for cell traces and \verb{BG_*} for background traces}

\item{method}{Correction method: "modern" (default) or "legacy"}

\item{span}{Smoothing span for baseline estimation (0.01-1.0). Default 0.45}

\item{normalize}{Whether to normalize traces by their mean. Default TRUE}

\item{verbose}{Whether to show progress messages. Default TRUE}
}
\value{
A data frame with corrected traces and a Time column
}
\description{
Performs background subtraction, normalization, and baseline correction
on calcium imaging traces. This is the main correction function that
consolidates both modern and legacy approaches.
}
\examples{
# Basic usage
raw <- generate_synthetic_data(3, 500)
corrected <- calcium_correction(raw)

# Custom parameters
corrected <- calcium_correction(raw, span = 0.3, normalize = FALSE)

# Legacy method
corrected <- calcium_correction(raw, method = "legacy")

}
