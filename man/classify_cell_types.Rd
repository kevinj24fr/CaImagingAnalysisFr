% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_classifiers.R
\name{classify_cell_types}
\alias{classify_cell_types}
\title{Classify cell types from activity patterns}
\usage{
classify_cell_types(
  traces,
  labels = NULL,
  method = "ranger",
  features = "all",
  train_fraction = 0.7,
  ...
)
}
\arguments{
\item{traces}{Matrix of traces (cells x time) or features (cells x features)}

\item{labels}{Factor or character vector of known cell type labels (for training)}

\item{method}{Classification method: "ranger", "xgboost", "svm", "glmnet"}

\item{features}{Character vector of features to extract, or NULL if traces
already contains features. Options: "temporal", "spectral", "statistical", "all"}

\item{train_fraction}{Fraction of data for training (rest for validation)}

\item{...}{Additional arguments passed to the classifier}
}
\value{
List with model, predictions, accuracy, and feature importance
}
\description{
Uses classical ML to classify neurons into functional types based on
their calcium activity features.
}
\examples{
\dontrun{
# With known labels for some cells
labels <- c("excitatory", "inhibitory", "excitatory", NA, NA, "inhibitory", NA)
result <- classify_cell_types(traces, labels, method = "ranger")

# Predict unlabeled cells
predicted <- result$predictions
}
}
