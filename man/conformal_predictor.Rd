% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal_prediction.R
\name{conformal_predictor}
\alias{conformal_predictor}
\title{Fit Conformal Predictor}
\usage{
conformal_predictor(
  model,
  X_calib,
  y_calib,
  method = c("split", "full", "cv"),
  alpha = 0.1,
  score_fn = NULL
)
}
\arguments{
\item{model}{Fitted model (any model with predict method)}

\item{X_calib}{Calibration features (n_calib x features)}

\item{y_calib}{Calibration responses}

\item{method}{Conformal method ("split", "full", "cv")}

\item{alpha}{Significance level (1 - alpha = coverage)}

\item{score_fn}{Custom score function (optional)}
}
\value{
Conformal predictor object
}
\description{
Create a conformal predictor with distribution-free coverage guarantees.
}
\examples{
\dontrun{
# Train a decoder, then calibrate for uncertainty
decoder <- decode_behavior(neural_features, behavior)
conf <- conformal_predictor(decoder, X_calib, y_calib, alpha = 0.1)

# Get prediction intervals
pred <- conformal_predict(conf, X_test)
# pred$lower and pred$upper have 90\% coverage guarantee
}
}
