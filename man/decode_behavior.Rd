% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_classifiers.R
\name{decode_behavior}
\alias{decode_behavior}
\title{Decode behavior from neural activity}
\usage{
decode_behavior(
  traces,
  behavior,
  method = "ridge",
  cv_folds = 5,
  time_lags = 0,
  transpose = FALSE,
  ...
)
}
\arguments{
\item{traces}{Matrix of traces (cells x time) or (time x cells)}

\item{behavior}{Vector or matrix of behavioral variables to predict}

\item{method}{Decoding method: "ridge", "lasso", "elastic_net", "ranger", "xgboost"}

\item{cv_folds}{Number of cross-validation folds}

\item{time_lags}{Integer vector of time lags to include (e.g., -2:2)}

\item{transpose}{If TRUE, traces is (time x cells); if FALSE, (cells x time)}

\item{...}{Additional arguments to the method}
}
\value{
List with model, predictions, R-squared, and weights
}
\description{
Predict behavioral variables from calcium traces using classical ML.
}
\examples{
\dontrun{
# Decode velocity from neural activity
result <- decode_behavior(traces, velocity, method = "ridge")

# With time lags
result <- decode_behavior(traces, position, method = "elastic_net", time_lags = -3:3)
}
}
