% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motion_correction.R
\name{motion_correct}
\alias{motion_correct}
\title{Motion Correction}
\usage{
motion_correct(
  movie,
  method = c("template", "piecewise", "optical_flow"),
  template = NULL,
  template_frames = NULL,
  max_shift = 20,
  upsample_factor = 10,
  batch_size = 500,
  verbose = TRUE
)
}
\arguments{
\item{movie}{3D array \link{height x width x frames} or path to TIFF file}

\item{method}{Registration method ("template", "piecewise", "optical_flow")}

\item{template}{Reference template (NULL = compute from data)}

\item{template_frames}{Frames to use for template computation}

\item{max_shift}{Maximum allowed shift in pixels}

\item{upsample_factor}{Subpixel precision (1 = pixel, 10 = 0.1 pixel)}

\item{batch_size}{Process frames in batches (memory management)}

\item{verbose}{Show progress messages}
}
\value{
List with corrected movie and motion parameters
}
\description{
Correct motion artifacts in calcium imaging data using template-based
or frame-by-frame registration.
}
\details{
Available methods:
\describe{
\item{template}{Rigid registration to a template image using cross-correlation}
\item{piecewise}{Non-rigid correction by dividing frame into patches}
\item{optical_flow}{Dense optical flow estimation (requires OpenCV)}
}
}
\examples{
\dontrun{
# Load movie
movie <- read_tiff_stack("recording.tif")

# Basic motion correction
result <- motion_correct(movie)

# With custom template
template <- apply(movie[,,1:100], c(1,2), mean)
result <- motion_correct(movie, template = template)

# Access results
corrected_movie <- result$corrected
shifts <- result$shifts
}

}
