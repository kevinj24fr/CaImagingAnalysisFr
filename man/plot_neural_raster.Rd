% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publication_plots.R
\name{plot_neural_raster}
\alias{plot_neural_raster}
\title{Neural Population Raster with State Annotations}
\usage{
plot_neural_raster(
  traces,
  frame_rate = 10,
  states = NULL,
  events = NULL,
  sort_by = "activity",
  normalize = "zscore",
  show_colorbar = TRUE,
  time_range = NULL
)
}
\arguments{
\item{traces}{Trace matrix (cells x time) or CalciumTraces object}

\item{frame_rate}{Frame rate in Hz}

\item{states}{Optional data frame with columns: start, end, state}

\item{events}{Optional vector of event times (in seconds)}

\item{sort_by}{How to sort cells: "none", "activity", "peak_time", "correlation"}

\item{normalize}{Normalize each cell: "zscore", "minmax", "none"}

\item{show_colorbar}{Show color scale bar}

\item{time_range}{Time range to plot (c(start, end) in seconds)}
}
\value{
A ggplot object
}
\description{
Creates a publication-quality raster plot showing population activity
with optional behavioral state annotations and event markers.
}
\examples{
\dontrun{
# Basic raster
plot_neural_raster(traces, frame_rate = 10)

# With behavioral states
states <- data.frame(start = c(0, 30, 60), end = c(30, 60, 90),
                     state = c("baseline", "stimulus", "recovery"))
plot_neural_raster(traces, frame_rate = 10, states = states)
}
}
