% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cell_registration.R
\name{register_cells}
\alias{register_cells}
\title{Register Cells Across Sessions}
\usage{
register_cells(
  sessions,
  method = c("centroid", "overlap", "correlation", "hungarian"),
  max_distance = 10,
  min_overlap = 0.3,
  reference_session = 1,
  alignment = NULL,
  verbose = TRUE
)
}
\arguments{
\item{sessions}{List of session data, each containing ROI information}

\item{method}{Registration method ("centroid", "overlap", "correlation", "hungarian")}

\item{max_distance}{Maximum allowed distance between matched cells (pixels)}

\item{min_overlap}{Minimum required overlap ratio (for "overlap" method)}

\item{reference_session}{Which session to use as reference (default: 1)}

\item{alignment}{Optional spatial alignment transforms between sessions}

\item{verbose}{Show progress messages}
}
\value{
List with registration results
}
\description{
Match cells detected in different imaging sessions based on spatial location
and other features.
}
\details{
Each session should be a list with at least one of:
\describe{
\item{rois}{List of ROI masks or labeled matrix}
\item{centroids}{Matrix of \link{x, y} centroid coordinates}
\item{mean_image}{Mean projection image (for correlation method)}
}

Methods:
\describe{
\item{centroid}{Match based on centroid distance}
\item{overlap}{Match based on spatial overlap of ROIs}
\item{correlation}{Match based on spatial correlation of ROI footprints}
\item{hungarian}{Optimal assignment using Hungarian algorithm}
}
}
\examples{
\dontrun{
# Register cells from two sessions
session1 <- list(centroids = centroids_day1, rois = rois_day1)
session2 <- list(centroids = centroids_day2, rois = rois_day2)

registration <- register_cells(
  sessions = list(day1 = session1, day2 = session2),
  method = "centroid",
  max_distance = 10
)

# Get matched cells
matches <- registration$matches
}

}
