% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistical_representation.R
\name{wavelet_denoise}
\alias{wavelet_denoise}
\alias{transformer_denoising}
\title{Wavelet Denoising}
\usage{
wavelet_denoise(
  traces,
  noise_level = 0.1,
  method = "wavelet",
  smooth_window = 5,
  ...
)

transformer_denoising(...)
}
\arguments{
\item{traces}{Matrix of noisy calcium traces (cells x time)}

\item{noise_level}{Estimated noise level for wavelet threshold (default: 0.1)}

\item{method}{Denoising method: "wavelet" or "smooth" (default: "wavelet")}

\item{smooth_window}{Window size for moving average if using "smooth" (default: 5)}

\item{...}{Additional arguments}
}
\value{
List containing denoised traces and metrics
}
\description{
Denoise calcium traces using wavelet thresholding or moving average smoothing.
Falls back to simple smoothing if waveslim package is not available.
}
\examples{
\dontrun{
traces <- matrix(rnorm(10 * 1000), nrow = 10)
result <- wavelet_denoise(traces, method = "smooth")
}
}
\keyword{internal}
